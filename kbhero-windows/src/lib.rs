// Windows detection engine — Milestone 2 (SetWinEventHook + UIAutomation).
// Stub only.

pub struct WindowsDetectionEngine;

impl WindowsDetectionEngine {
    pub fn new() -> Self {
        Self
    }
}
